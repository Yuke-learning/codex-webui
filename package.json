{
  "name": "codex-webui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A local-only web console for managing Codex app-server threads.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test"
  }
}
