# Repository workflow

- After completing any user-requested code or documentation change, run the relevant verification, commit the change, and push the current branch to `origin` before handing off.
- Use the verified GitHub identity `Yuke-learning <149859004+Yuke-learning@users.noreply.github.com>` for commits.
- Never commit API keys, bearer tokens, SSH keys, Codex configuration, local thread data, logs, or `.env` files. Keep `.gitignore` aligned with any new runtime artifacts.
- If a commit or push cannot be completed, explain the precise blocker rather than claiming the work is synchronized.
